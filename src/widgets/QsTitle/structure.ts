import { Widgets } from '@/type/widgets/index'

const buildObj: Widgets = {
    icon: 'FontSizeOutlined',
    name: '标题',
    fields: {
        value: '默认标题',
        style: {
        },
        test: {}
    }
}

export default buildObj
